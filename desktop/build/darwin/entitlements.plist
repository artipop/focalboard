<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements used when code-signing the app with the hardened runtime
  (required for notarization). The in-process Focalboard server binds a local
  TCP socket (network.server) and makes outbound requests such as telemetry
  (network.client). Signing:
    codesign --deep --force --options runtime \
      --entitlements build/darwin/entitlements.plist \
      --sign "Developer ID Application: ..." Focalboard.app
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.network.server</key>
  <true/>
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
</dict>
</plist>
